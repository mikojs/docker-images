name: build
on: [push]
jobs:
  build-images:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: build images
        run: |
          cd docker-compose
          docker compose \
            -f build.yml \
            build
